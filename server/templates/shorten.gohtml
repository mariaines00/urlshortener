<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Shorten url</title>
    <style>
        html, body {
            font-family: sans-serif;
            padding: 0;
            border: 0;
            margin: 0;
        }

        body, form {
            display: flex;
            flex-flow: column nowrap;
            justify-content: center;
            align-items: center;
        }

        .input-text {
            height: 45px;
            background: #fff;
            border: 1px solid #d3d4d7;
            border-radius: 8px 8px 8px 8px;
            font-size: 20px;
            color: #36383b;
            padding: 0 20px;
        }

        .button {
            background: chocolate;
            color: white;
            width: 100%;
            padding: 15px;
            border-radius: 8px;
            border: 1px solid transparent;
        }

    </style>
</head>
<body>

<h1>Shorten URL</h1>
<form method="post" action="short/new">
    <input class="input-text" type="url" name="url" placeholder="url" required autofocus>
    <input class="button" type="submit" value="Shortnen">
</form>

</body>
</html>
