<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Shorten url</title>
     <style>
        html, body {
            font-family: sans-serif;
            font-size: 15px;
            color: #36383b;
            padding: 0;
            border: 0;
            margin: 0;
            background: seashell;
        }

        h2 {
            font-weight: 400;
        }

        h3 {
            margin-top: 0;
            font-weight: 300;
        }

        body {
            display: flex !important;
            flex-flow: column nowrap;
            justify-content: center;
            align-items: center;
        }

        .results {
            background: white;
            padding: 25px;
            border: 1px solid #d3d4d7;
            border-radius: 8px 8px 8px 8px;
            font-weight: 200;
        }
    </style>
</head>
<body>
    <h2> New short url sucessfully created for </h2>
    <h3> {{.OutsideAddr}} </h3>
    <div class="results">
        <p> {{.Path}} </p>
    </div>
</body>
</html>
